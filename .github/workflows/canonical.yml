name: Release canonical

on:
  push:
    branches:
      - main

  # Runs at 00:00, only on Sunday
  schedule:
    - cron: '0 0 * * SUN'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      # Generate modules
      - uses: actions/checkout@v3
      - id: module
        run: echo "module=$(jq -cr '' nginx-modules.json)" >> "$GITHUB_OUTPUT"
      - id: release
        run: echo 'version=["stable", "mainline"]' >> "$GITHUB_OUTPUT"
    outputs:
      version: ${{ steps.release.outputs.version }}
      module: ${{ steps.module.outputs.module }}


  alpine:
    runs-on: ubuntu-latest
    # continue-on-error: true
    needs: generate
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        version: ${{ fromJson(needs.generate.outputs.version) }}
        module: ${{ fromJson(needs.generate.outputs.module) }}
    steps:
      - uses: actions/checkout@v3
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: chocolatefrappe/nginx-modules
          tags: type=raw,value=${{ matrix.version }}-alpine-${{ matrix.module }}
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push
        uses: docker/bake-action@v3
        env:
          NGINX_VERSION: ${{ matrix.version }}
          ENABLED_MODULE: ${{ matrix.module }}
        with:
          targets: alpine
          files: |
            docker-bake.hcl
            ${{ steps.meta.outputs.bake-file }}
          push: ${{ github.event_name != 'pull_request' }}
      - name: Docker Metadata Summary
        uses: dockerbakery/docker-metadata-summary@v1

  debian:
    runs-on: ubuntu-latest
    # continue-on-error: true
    needs: generate
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        version: ${{ fromJson(needs.generate.outputs.version) }}
        module: ${{ fromJson(needs.generate.outputs.module) }}
    steps:
      - uses: actions/checkout@v3
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: chocolatefrappe/nginx-modules
          tags: type=raw,value=${{ matrix.version }}-${{ matrix.module }}
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push
        uses: docker/bake-action@v3
        env:
          NGINX_VERSION: ${{ matrix.version }}
          ENABLED_MODULE: ${{ matrix.module }}
        with:
          targets: debian
          files: |
            docker-bake.hcl
            ${{ steps.meta.outputs.bake-file }}
          push: ${{ github.event_name != 'pull_request' }}
      - name: Docker Metadata Summary
        uses: dockerbakery/docker-metadata-summary@v1
